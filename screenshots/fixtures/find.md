## Find and Replace

⌘F searches the whole document, not only the
lines currently on screen.

- Match case, whole word, regular expression
- Replace one match, or all of them
- ⌘D selects the next occurrence

Escape closes the panel and hands the document
back to the cursor.
