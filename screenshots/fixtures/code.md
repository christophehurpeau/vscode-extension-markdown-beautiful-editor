## Code

Inline `code` keeps its backticks.

```typescript
export function greet(name: string): string {
    const greeting = `Hello, ${name}!`;
    console.log(greeting);
    return greeting;
}
```

```python
def hello_world(times: int = 1) -> None:
    for _ in range(times):
        print("Hello, World!")
```
